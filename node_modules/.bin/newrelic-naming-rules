#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../newrelic/bin/test-naming-rules.js" "$@"
  ret=$?
else 
  node  "$basedir/../newrelic/bin/test-naming-rules.js" "$@"
  ret=$?
fi
exit $ret
